[versions]
# Plugins
fabricLoom = "1.17-SNAPSHOT"
spotless = "8.10.2"

# Libraries
minecraft = "26.3"
fabricLoader = "0.19.5" # on change - check if fabric.mod.json needs to be increased
fabricApi = "0.160.5+26.3"

[libraries]
minecraft = { group = "com.mojang", name = "minecraft", version.ref = "minecraft" }
fabricLoader = { group = "net.fabricmc", name = "fabric-loader", version.ref = "fabricLoader" }

[plugins]
fabricLoom = { id = "net.fabricmc.fabric-loom", version.ref = "fabricLoom" }
spotless = { id = "com.diffplug.spotless", version.ref = "spotless" }

[bundles]
